# Unsorted Test Report

The following table summarizes what each `.io` script in `tests/unsorted/` is asserting.

| Test file | Behaviour under test | Suggested new location |
| --- | --- | --- |
| `20180412_113656.io` | Nested `try`/`finally` blocks where every branch performs a `return`, confirming that the value from the outermost `finally` (`x = 3; return x * 3`) overrides prior `return` expressions and observes the last mutation of `x`. | `conforming/TryFinallyReturnOverride.io` |
| `20180412_114017.io` | Using a `with` statement to enter an object scope and access a property (`s`) without qualifying it, ensuring that `with` exposes object members as variables. | `conforming/WithStatementObjectBinding.io` |
| `20180412_122400.io` | Global `var` declarations versus `eval("var ...")` and `function` declarations, checking hoisting/initialisation order and that later declarations (`var a = 3`, `function a() {}`) overwrite earlier values while preserving previously observed outputs. | `conforming/HoistedVarEvalOverwrite.io` |
| `20180412_123214.io` | Deleting globals created through different mechanisms, demonstrating that `delete a` (declared with `var`) is not allowed while `delete b` (introduced via `eval`) succeeds. | `conforming/DeleteGlobalBindingOrigins.io` |
| `20180412_231138.io` | Parser edge cases for `for`-loops: distinguishing `for (var i = 'x' in ...)` (a valid `for-in` initializer) from the parenthesised expression form that should throw a syntax error, plus basic valid `for`/`for-in` declarations. | `regression/ForInInitializerSyntax20180412.io` |
| `20180412_231343.io` | Interaction of `eval` with `try`/`catch`/`finally`: propagating caught values, ensuring `finally` runs, and confirming the expression result reported by `eval` when all arms finish. | `conforming/EvalTryCatchFinallyPropagation.io` |
| `20180413_020542.io` | `switch` statement behaviour across many scenarios—fall-through, absence of `default`, nested `switch` usage inside `eval`, and correct handling of `break`, `return`, and expression values. | `conforming/SwitchFlowMatrix.io` |
| `20180413_021133.io` | Returning from inside `switch` cases inside a function, verifying that `return` values propagate correctly and that missing cases yield `undefined`. | `conforming/SwitchReturnPropagation.io` |
| `20180413_023104.io` | Labelled `switch` statements: breaking from outer labels, returning specific values, and ensuring nested `switch` constructs respect labels. | `conforming/LabeledSwitchBreakSemantics.io` |
| `20180413_023357.io` | A labelled block around a `switch` that executes a `break` from within an inner `switch`, confirming the label is honoured. | `conforming/LabeledBlockInnerSwitchBreak.io` |
| `20180413_024603.io` | `try`/`finally` surrounding a labelled `switch`, proving that `finally` executes and can itself perform a `break` from the outer label. | `conforming/TryFinallyLabeledSwitchBreak.io` |
| `20180413_025917.io` | Variations of the previous case including calling through a function and introducing nested `try`/`finally`, making sure both `break` and `return` interact correctly with label exits and `finally` blocks. | `conforming/TryFinallyLabelBreakReturn.io` |
| `20180413_030535.io` | `return` statements within nested `try`/`finally` blocks inside immediately invoked functions, validating how `return`, `break`, and `finally` interplay when code is executed via function invocation. | `conforming/NestedTryFinallyReturnIIFE.io` |
| `20180413_034336.io` | Executing `break` from inside a `finally`, and verifying whether `return` expressions inside `try`/`finally` end up yielding the value from the `finally` or the original `try`. | `conforming/FinallyBreakReturnValue.io` |
| `20180413_153804.io` | An exhaustive matrix of labelled blocks (`out:`) with `try`/`finally` combinations to confirm evaluation order of trailing expressions, `break`, `return`, and `finally` overrides under both direct `eval` and function invocation. | `conforming/LabeledTryFinallyMatrix.io` |
| `20180413_160609.io` | Behaviour of `try`/`catch`/`finally` with thrown values: ensuring catches run (and can `break` labels), `finally` blocks execute even when rethrowing, and that surrounding expressions observe the correct value. | `conforming/TryCatchFinallyValuePropagation.io` |
| `20180413_212708.io` | Simple baseline regressions around returning values from immediately invoked functions and `switch` statements, establishing expected `return` semantics. | `conforming/IIFEAndSwitchReturnBaselines.io` |
| `20180414_025116.io` | Layers of `try`/`finally` inside a labelled block, verifying how chained `return` statements, empty `finally` bodies, and `break`/`return` from `finally` alter the function result. | `conforming/NestedLabelTryFinallyReturn.io` |
| `20180414_030616.io` | `for`-loops where `continue` is triggered from a `finally` clause, ensuring that the loop proceeds to the next iteration after executing `finally`, even when the body attempts to `return`/`throw`. | `conforming/ForLoopFinallyContinue.io` |
| `20180416_130246.io` | Using `with` together with `eval` to resolve identifiers, confirming that referencing an undefined variable throws and that bindings introduced by `with` remain accessible. | `conforming/WithEvalIdentifierResolution.io` |
| `20180416_130544.io` | Verifying that evaluating an identifier before `with` binding occurs (`y; with (...)`) throws a `ReferenceError`. | `conforming/WithBindingReferenceError.io` |
| `20180416_170605.io` | `for-in` iteration semantics: the value returned from surrounding expressions when iteration occurs or not, behaviour of labelled `break`/`continue`, and iterating over array indices. | `conforming/ForInControlFlowSemantics.io` |
| `20180416_183627.io` | Side effects of `for-in` loops that assign to array slots or object properties, including evaluation order of iterator expressions, label control flow, returns from within labels, and repeated evaluation of computed property names. | `conforming/ForInSideEffectOrdering.io` |
| `20180417_032712.io` | Confirming that `eval("var y = 3")` executed inside a `with` block creates a global binding (visible via `print(y)`) but does not define a property on the `with` object (`o.y`). | `conforming/WithEvalGlobalBindingOnly.io` |
| `20180425_004820.io` | Ensuring `eval("")` (both directly and nested) evaluates to `undefined`. | `conforming/EvalEmptyStringReturnsUndefined.io` |
| `20180430_172107.io` | Accessing object members through `this`, `with`, and `eval` in various contexts—including inside `try`/`catch`/`finally`—to ensure `this` binding and scope chains resolve the instance field `m`. | `conforming/ThisWithEvalPropertyLookup.io` |
| `20180430_173233.io` | Instantiating a constructor function with `new` and checking that the instance inherits a property initialised on `this`. | `conforming/ConstructorThisPropertyInitialisation.io` |
| `20180513_201614.io` | Behaviour of the `in` operator with sparse arrays: holes that were never assigned should fail `in`, while slots explicitly set (even to `undefined`) should succeed. | `conforming/SparseArrayInOperatorHoles.io` |
| `20180514_003134.io` | Array literal hole handling—`length` updates and element access results when arrays contain trailing commas or intentionally omitted elements. | `conforming/ArrayLiteralHoleLength.io` |
| `20180612_174152.io` | Precedence between `valueOf` and `toString` during string conversion, showing that concatenation (`'' + o`) uses `valueOf` while `String(o)` calls `toString`. | `conforming/ValueOfVsToStringPrecedence.io` |
| `20180613_174546.io` | Replacing the global `Number` constructor and demonstrating that invoking it with `new` executes the reassigned function, while `new Object(23)` remains unaffected. | `conforming/OverriddenNumberConstructorNew.io` |
| `20180615_003057.io` | Overriding `String.prototype` conversion hooks to validate which method (`valueOf`, `toString`) is used by direct method calls, concatenation, and `JSON.stringify`. | `conforming/StringPrototypeConversionHooks.io` |
| `20200119_014039.io` | Baseline invariants on `String.prototype`: its `length` property is numeric `0`, stringification behaves as expected, and replacing `toString` with `Object.prototype.toString` produces `[object String]`. | `conforming/StringPrototypeInvariants.io` |
